using Newtonsoft.Json;
using System;
using System.Net.Http;
using System.Text;
using System.Threading.Tasks;
using Xunit;

namespace Tests
{
    public class UnitTest1
    {
     
       /*[Fact]
        * public async void PutUser()
        {
            var client = new HttpClient();
            var url = "http://localhost:5001/api/users";
           // var user = new UserEntity();

            var json = JsonConvert.SerializeObject(user);
            var data = new StringContent(json, Encoding.UTF8, "application/json");
   

            var postImage = await client.PutAsync(url, data);
            var result = await postImage.Content.ReadAsStringAsync();

            //compare images using bytes length

           // Assert.Equal("OK", result.htt);
        }*/ 
    } 
}
